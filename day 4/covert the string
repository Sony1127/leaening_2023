#include <stdio.h>
#include <stdlib.h>

int main() {
    char str[100];
    
    printf("Enter a string representing an integer: ");
    fgets(str, sizeof(str), stdin);
    
    int number = atoi(str);
    
    printf("Converted integer: %d\n", number);
    
    return 0;
}